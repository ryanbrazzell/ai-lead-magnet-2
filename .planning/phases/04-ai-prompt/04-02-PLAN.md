---
phase: 04-ai-prompt
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - web/src/lib/ai/prompts/fallback-prompts.ts
  - web/src/lib/ai/prompts/__tests__/prompt-system.test.ts
autonomous: true

must_haves:
  truths:
    - "All three fallback prompts include coreTaskType instruction with the four valid values"
    - "All three fallback prompts request 24 tasks (8 per frequency) instead of 30 tasks (10 per frequency)"
    - "Prompt system tests pass with updated assertions matching the new prompt content"
    - "Tests verify coreTaskType presence and richer description instruction in the main prompt"
  artifacts:
    - path: "web/src/lib/ai/prompts/fallback-prompts.ts"
      provides: "Fallback prompts with coreTaskType instruction and aligned task counts"
      contains: "coreTaskType"
    - path: "web/src/lib/ai/prompts/__tests__/prompt-system.test.ts"
      provides: "Updated test suite verifying prompt content including coreTaskType"
      contains: "coreTaskType"
  key_links:
    - from: "web/src/lib/ai/prompts/fallback-prompts.ts"
      to: "web/src/types/task.ts"
      via: "coreTaskType values must match CoreTaskType union type"
      pattern: "emailManagement|calendarManagement|personalLifeManagement|businessProcessManagement"
    - from: "web/src/lib/ai/prompts/__tests__/prompt-system.test.ts"
      to: "web/src/lib/ai/prompts/time-freedom-prompt.ts"
      via: "test assertions on TIME_FREEDOM_PROMPT_JSON content"
      pattern: "toContain.*coreTaskType"
---

<objective>
Update all three fallback prompts to include coreTaskType instruction and align task counts to 24 (matching the main prompt and validator/fixer expectations), then update the prompt system tests to verify the new prompt content including coreTaskType and richer descriptions.

Purpose: Fallback prompts must request coreTaskType so that tasks generated via any prompt path include explicit Core Four classification. The test suite must be updated because it has stale assertions (references to 230+ line counts, sections like "CRITICAL DISTRIBUTION REQUIREMENT" that no longer exist in the current 98-line prompt).

Output: Modified fallback-prompts.ts with coreTaskType + aligned counts, updated prompt-system.test.ts with correct assertions.
</objective>

<execution_context>
@/Users/ryanbrazzell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanbrazzell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-prompt/04-RESEARCH.md
@.planning/phases/04-ai-prompt/04-01-SUMMARY.md

@web/src/lib/ai/prompts/fallback-prompts.ts
@web/src/lib/ai/prompts/__tests__/prompt-system.test.ts
@web/src/lib/ai/prompts/time-freedom-prompt.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update fallback prompts with coreTaskType and aligned task counts</name>
  <files>web/src/lib/ai/prompts/fallback-prompts.ts</files>
  <action>
Modify all three fallback prompt builder functions in `web/src/lib/ai/prompts/fallback-prompts.ts`:

**1. `buildSimplifiedPrompt` (line 51-67):**
- Change `"Generate exactly 30 tasks (10 daily, 10 weekly, 10 monthly)"` to `"Generate exactly 24 tasks (8 daily, 8 weekly, 8 monthly)"`
- Change the MANDATORY core EA areas list to include coreTaskType values:
```
MANDATORY: Include these 4 core EA areas:
1. Complete email management (daily EA task, coreTaskType: "emailManagement")
2. Calendar and scheduling management (daily EA task, coreTaskType: "calendarManagement")
3. Personal life coordination (weekly EA task, coreTaskType: "personalLifeManagement")
4. Business process management (monthly EA task, coreTaskType: "businessProcessManagement")
```
- After the MANDATORY section, add:
```
Every task must include a coreTaskType field: "emailManagement", "calendarManagement", "personalLifeManagement", or "businessProcessManagement".
```
- Change `"At least 12 tasks delegatable to EA (40% minimum)"` to `"At least 15 tasks delegatable to EA (63%)"` to match the main prompt's 5-EA-per-frequency ratio.
- Change `"Clear titles and detailed descriptions"` to `"Clear titles and 2-3 sentence descriptions with specific actions"`

**2. `buildEmergencyPrompt` (line 81-91):**
- Change `"Generate a standard EA Time Freedom Report with 30 tasks."` to `"Generate a standard EA Time Freedom Report with 24 tasks."`
- Change the structure section:
```
Structure:
- 8 daily tasks (include email management, calendar management)
- 8 weekly tasks (include personal life management)
- 8 monthly tasks (include business process management)
```
- Change `"At least 12 tasks must be EA-delegatable"` to `"At least 15 tasks must be EA-delegatable (isEA: true)."`
- Add at the end before the last line: `"Every task must include coreTaskType: emailManagement, calendarManagement, personalLifeManagement, or businessProcessManagement."`

**3. `buildStreamlinedPrompt` (line 124-131, the isNewForm ultra-optimized branch):**
- Change `"30 tasks (10 each: daily/weekly/monthly)"` to `"24 tasks (8 each: daily/weekly/monthly)"`
- Change the `Include:` line to:
```
Include coreTaskType on each task: emailManagement, calendarManagement, personalLifeManagement, or businessProcessManagement.
Include: Email Mgmt (daily EA), Calendar (daily EA), Personal Life (weekly EA), Process Mgmt (monthly EA).
```

Do NOT change the function signatures, exports, or log messages. Only change the prompt string content.
  </action>
  <verify>
Run: `cd /Users/ryanbrazzell/boundless-os-template-2 && grep -c 'coreTaskType' web/src/lib/ai/prompts/fallback-prompts.ts`
Expected: at least 3 (one per fallback prompt function).

Run: `cd /Users/ryanbrazzell/boundless-os-template-2 && grep '30 tasks' web/src/lib/ai/prompts/fallback-prompts.ts`
Expected: no matches (all instances replaced with 24).
  </verify>
  <done>
All three fallback prompt functions (buildSimplifiedPrompt, buildEmergencyPrompt, buildStreamlinedPrompt) include coreTaskType instruction with the four valid values. All references to "30 tasks" and "10 daily/weekly/monthly" are replaced with "24 tasks" and "8 daily/weekly/monthly".
  </done>
</task>

<task type="auto">
  <name>Task 2: Update prompt system tests for new prompt content</name>
  <files>web/src/lib/ai/prompts/__tests__/prompt-system.test.ts</files>
  <action>
Rewrite the first test (`'contains the exact 240-line prompt with required sections'`) in `web/src/lib/ai/prompts/__tests__/prompt-system.test.ts` to match the actual current prompt content after Plan 04-01's modifications.

**Remove these stale assertions** (they reference sections that do not exist in the current prompt):
- `toContain('CRITICAL DISTRIBUTION REQUIREMENT')` -- does not exist
- `toContain('DAILY TASKS MIX (10 total)')` -- does not exist (prompt says 8 per category)
- `toContain('WEEKLY TASKS MIX (10 total)')` -- does not exist
- `toContain('MONTHLY TASKS MIX (10 total)')` -- does not exist
- `toContain('EA CAN HANDLE')` -- does not exist
- `toContain('FOUNDER MUST HANDLE')` -- does not exist
- `toContain('FINAL VALIDATION CHECKLIST')` -- does not exist
- `toContain('EMAIL MANAGEMENT')` -- the CORE TASK CLASSIFICATION section uses lowercase ("emailManagement"), not uppercase
- `toContain('CALENDAR MANAGEMENT')` -- same
- `toContain('PERSONAL LIFE MANAGEMENT')` -- same
- `toContain('BUSINESS PROCESS MANAGEMENT')` -- same
- The line count assertion `expect(lineCount).toBeGreaterThanOrEqual(230)` and `toBeLessThanOrEqual(250)` -- the prompt is ~120-140 lines after Phase 4 changes

**Replace with these correct assertions:**

```typescript
it('contains required sections and coreTaskType classification', () => {
  expect(TIME_FREEDOM_PROMPT_JSON).toBeDefined();
  expect(typeof TIME_FREEDOM_PROMPT_JSON).toBe('string');

  // Core structure sections
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('You are a professional Executive Assistant');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('{LEAD_CONTEXT}');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('YOUR TASK');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('TASK GUIDELINES');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('PERSONALIZATION');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('TASK FORMAT');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('OUTPUT JSON');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('REQUIREMENTS');

  // Task count requirements preserved
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('Exactly 24 tasks total');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('8 daily, 8 weekly, 8 monthly');

  // Phase 4: coreTaskType classification
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('CORE TASK CLASSIFICATION');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('coreTaskType');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('emailManagement');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('calendarManagement');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('personalLifeManagement');
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('businessProcessManagement');

  // Phase 4: richer descriptions
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('2-3 sentences');

  // Must NOT contain isCoreEATask (per research decision)
  expect(TIME_FREEDOM_PROMPT_JSON).not.toContain('isCoreEATask');

  // Output must be valid JSON only
  expect(TIME_FREEDOM_PROMPT_JSON).toContain('Output ONLY valid JSON, no other text');

  // Line count sanity check (prompt is ~100-150 lines after Phase 4)
  const lineCount = TIME_FREEDOM_PROMPT_JSON.split('\n').length;
  expect(lineCount).toBeGreaterThanOrEqual(90);
  expect(lineCount).toBeLessThanOrEqual(160);
});
```

Keep ALL OTHER tests in the file unchanged (serializeLeadData, buildUnifiedPromptJSON, Engagement Level). They test different functions and are not stale.
  </action>
  <verify>
Run: `cd /Users/ryanbrazzell/boundless-os-template-2/web && npx vitest run src/lib/ai/prompts/__tests__/prompt-system.test.ts --reporter=verbose`

All tests must pass. Zero failures.
  </verify>
  <done>
The prompt system test file has updated assertions that match the actual prompt content after Phase 4 changes. The test verifies: (1) all required prompt sections exist, (2) coreTaskType classification section with all four values, (3) 2-3 sentence description instruction, (4) 24-task structure preserved, (5) isCoreEATask is NOT present. All other tests (serializeLeadData, buildUnifiedPromptJSON, Engagement Level) remain unchanged and pass.
  </done>
</task>

</tasks>

<verification>
1. All three fallback prompts contain "coreTaskType" instruction
2. No fallback prompt references "30 tasks" -- all say "24 tasks"
3. `npx vitest run src/lib/ai/prompts/__tests__/prompt-system.test.ts` passes with zero failures
4. TypeScript compilation passes: `cd web && npx tsc --noEmit`
</verification>

<success_criteria>
- All fallback prompts request coreTaskType with the four exact CoreTaskType values
- All fallback prompts request 24 tasks (8 per frequency), not 30
- Prompt system tests pass and verify coreTaskType presence in the main prompt
- No regressions in other test assertions (serializeLeadData, buildUnifiedPromptJSON, engagement level)
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-prompt/04-02-SUMMARY.md`
</output>
