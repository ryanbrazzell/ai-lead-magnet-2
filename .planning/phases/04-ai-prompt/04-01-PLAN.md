---
phase: 04-ai-prompt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/ai/prompts/time-freedom-prompt.ts
  - web/src/lib/ai/claude-client.ts
autonomous: true

must_haves:
  truths:
    - "The AI prompt explicitly requests a coreTaskType field on every generated task"
    - "The AI prompt requests 2-3 sentence descriptions instead of 15-25 word descriptions"
    - "The AI prompt retains the 24-task / 8-per-frequency structure"
    - "The maxTokens value is 8192 to accommodate larger AI output"
  artifacts:
    - path: "web/src/lib/ai/prompts/time-freedom-prompt.ts"
      provides: "Main prompt with coreTaskType instruction and richer description guidance"
      contains: "coreTaskType"
    - path: "web/src/lib/ai/claude-client.ts"
      provides: "Claude config with increased token budget"
      contains: "maxTokens: 8192"
  key_links:
    - from: "web/src/lib/ai/prompts/time-freedom-prompt.ts"
      to: "web/src/types/task.ts"
      via: "coreTaskType field values must match CoreTaskType union type"
      pattern: "emailManagement|calendarManagement|personalLifeManagement|businessProcessManagement"
---

<objective>
Update the main AI prompt to request explicit coreTaskType classification and richer 2-3 sentence descriptions on every generated task, and increase the Claude API maxTokens from 4096 to 8192 to accommodate the larger output.

Purpose: Phase 3's Core Four task grouping currently relies on keyword inference. Adding coreTaskType directly from the AI improves classification accuracy. Richer descriptions improve PDF content quality. The token budget increase prevents JSON truncation.

Output: Modified time-freedom-prompt.ts with coreTaskType + description upgrades, modified claude-client.ts with maxTokens: 8192.
</objective>

<execution_context>
@/Users/ryanbrazzell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanbrazzell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ai-prompt/04-RESEARCH.md

@web/src/lib/ai/prompts/time-freedom-prompt.ts
@web/src/lib/ai/claude-client.ts
@web/src/types/task.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add coreTaskType and richer descriptions to main prompt</name>
  <files>web/src/lib/ai/prompts/time-freedom-prompt.ts</files>
  <action>
Modify TIME_FREEDOM_PROMPT_JSON in `web/src/lib/ai/prompts/time-freedom-prompt.ts` with these specific changes:

1. **Add a CORE TASK CLASSIFICATION section** after the PERSONALIZATION section (before TASK FORMAT). Insert:
```
===== CORE TASK CLASSIFICATION =====
Every task MUST include a coreTaskType field indicating which Core Four area it belongs to:
- "emailManagement": Email inbox, correspondence, filtering, responses
- "calendarManagement": Scheduling, meetings, appointments, time management
- "personalLifeManagement": Travel, family, personal errands, vendor coordination, personal appointments
- "businessProcessManagement": SOPs, workflows, reporting, CRM, recurring operations, automation

Choose the BEST fit for each task. When in doubt, use "businessProcessManagement" (broadest category).
Tasks are grouped by frequency (daily/weekly/monthly), NOT by coreTaskType.
```

2. **Update the description instruction** in the TASK FORMAT section. Change:
```
- description: 15-25 words explaining what this involves
```
to:
```
- description: 2-3 sentences (40-60 words) explaining what this involves, with specific actions and business outcomes. Be detailed and reference the founder's context.
```

3. **Add coreTaskType to the task field list** in the TASK FORMAT section. After the `category` line, add:
```
- coreTaskType: One of emailManagement|calendarManagement|personalLifeManagement|businessProcessManagement
```

4. **Update the JSON example task** in the OUTPUT JSON section. Replace the current example:
```json
{
  "title": "Priority Inbox Zero Maintenance",
  "description": "Processing and organizing incoming emails, flagging urgent items, drafting responses, and maintaining inbox at zero.",
  "owner": "EA",
  "isEA": true,
  "category": "Communication"
}
```
with:
```json
{
  "title": "Priority Inbox Zero Maintenance",
  "description": "Processing and organizing all incoming emails, triaging into priority folders, and drafting responses using your voice. Your inbox stays at zero so you never waste time sorting through messages yourself.",
  "owner": "EA",
  "isEA": true,
  "category": "Communication",
  "coreTaskType": "emailManagement"
}
```

5. **Add coreTaskType requirement** to the REQUIREMENTS section. Add this line after the existing requirements (before the final "Output ONLY valid JSON" line):
```
- Every task must include a coreTaskType field (emailManagement, calendarManagement, personalLifeManagement, or businessProcessManagement)
```

Do NOT add `isCoreEATask` to the prompt -- per research, it is not needed.
Do NOT change the task count (24 total, 8 per frequency, 5 EA + 3 Founder per frequency).
Do NOT change the ea_task_percent or ea_task_count values.
  </action>
  <verify>
Run: `cd /Users/ryanbrazzell/boundless-os-template-2 && npx tsx -e "const { TIME_FREEDOM_PROMPT_JSON } = require('./web/src/lib/ai/prompts/time-freedom-prompt'); console.log(TIME_FREEDOM_PROMPT_JSON.includes('coreTaskType') ? 'PASS: coreTaskType found' : 'FAIL'); console.log(TIME_FREEDOM_PROMPT_JSON.includes('2-3 sentences') ? 'PASS: rich description found' : 'FAIL'); console.log(TIME_FREEDOM_PROMPT_JSON.includes('Exactly 24 tasks') ? 'PASS: 24 task count preserved' : 'FAIL');"`

Also verify the prompt does NOT contain `isCoreEATask`.
  </verify>
  <done>
The TIME_FREEDOM_PROMPT_JSON string contains: (1) a CORE TASK CLASSIFICATION section with all four coreTaskType values, (2) description instruction requesting 2-3 sentences, (3) coreTaskType in the task field list, (4) JSON example with coreTaskType field, (5) coreTaskType requirement in REQUIREMENTS. The 24-task structure is unchanged. isCoreEATask is NOT mentioned.
  </done>
</task>

<task type="auto">
  <name>Task 2: Increase maxTokens from 4096 to 8192</name>
  <files>web/src/lib/ai/claude-client.ts</files>
  <action>
In `web/src/lib/ai/claude-client.ts`, make two changes:

1. **Update CLAUDE_CONFIG.maxTokens** (line 57): Change `maxTokens: 4096` to `maxTokens: 8192`.

2. **Update the file header comment** (line 10): Change `Max Output Tokens: 4096` to `Max Output Tokens: 8192`.

No other changes to this file. The model, temperature, timeout, and maxRetries remain the same.
  </action>
  <verify>
Run: `cd /Users/ryanbrazzell/boundless-os-template-2 && grep -n 'maxTokens' web/src/lib/ai/claude-client.ts`

Expected output: line with `maxTokens: 8192` (not 4096).
  </verify>
  <done>
CLAUDE_CONFIG.maxTokens equals 8192. The file header comment reflects 8192. No other values in the config changed.
  </done>
</task>

</tasks>

<verification>
1. `TIME_FREEDOM_PROMPT_JSON` contains "coreTaskType" (at least 5 occurrences: classification section, field list, example, requirement, section header)
2. `TIME_FREEDOM_PROMPT_JSON` contains "2-3 sentences" in the description instruction
3. `TIME_FREEDOM_PROMPT_JSON` still contains "Exactly 24 tasks total"
4. `TIME_FREEDOM_PROMPT_JSON` does NOT contain "isCoreEATask"
5. `CLAUDE_CONFIG.maxTokens` is 8192
6. TypeScript compilation passes: `cd web && npx tsc --noEmit`
</verification>

<success_criteria>
- The main prompt requests coreTaskType on every task with exact CoreTaskType union values
- The main prompt requests 2-3 sentence descriptions
- The 24-task / 8-per-frequency structure is explicitly preserved in the prompt
- maxTokens is increased to 8192 for token safety
- No references to isCoreEATask in the prompt
</success_criteria>

<output>
After completion, create `.planning/phases/04-ai-prompt/04-01-SUMMARY.md`
</output>
