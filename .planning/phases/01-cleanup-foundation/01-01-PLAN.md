---
phase: 01-cleanup-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/src/lib/pdf/generator.ts
  - web/src/lib/pdf/layout.ts
  - web/src/lib/pdf/__tests__/generator.test.ts
  - web/src/lib/pdf/__tests__/layout.test.ts
  - web/src/lib/pdf/__tests__/integration.test.ts
  - web/src/lib/pdf/index.ts
  - web/scripts/generate-sample-pdf.ts
  - web/scripts/send-test-email.ts
  - web/src/app/api/generate-pdf/__tests__/route.test.ts
  - web/src/lib/pdf/layout-v2.ts
autonomous: true

must_haves:
  truths:
    - "V1 generator and layout files (generator.ts, layout.ts) no longer exist in the codebase"
    - "V1 test files (generator.test.ts, layout.test.ts, integration.test.ts) no longer exist in the codebase"
    - "All imports that referenced V1 files compile without errors after cleanup"
    - "All PDF color usage in layout-v2.ts goes through pre-computed RGB tuple constants — zero setColor calls remain"
    - "The setColor function and hex-string COLORS object no longer exist in layout-v2.ts"
    - "The color palette includes constants for cover, framework, each Core Four area accent, and CTA"
  artifacts:
    - path: "web/src/lib/pdf/layout-v2.ts"
      provides: "RGB tuple color constants and direct jsPDF color calls"
      contains: "as const satisfies RGB"
    - path: "web/src/lib/pdf/index.ts"
      provides: "Barrel export updated to reference V2 only"
  key_links:
    - from: "web/src/lib/pdf/index.ts"
      to: "web/src/lib/pdf/layout-v2.ts"
      via: "re-export (no V1 references)"
      pattern: "from.*layout-v2"
    - from: "web/scripts/generate-sample-pdf.ts"
      to: "web/src/lib/pdf/generator-v2.ts"
      via: "import"
      pattern: "from.*generator-v2"
    - from: "web/src/app/api/generate-pdf/__tests__/route.test.ts"
      to: "web/src/lib/pdf/generator-v2.ts"
      via: "mock import"
      pattern: "generator-v2"
---

<objective>
Delete all V1 PDF dead code (5 files), clean up all V1 references (4 files), and replace the hex-parsing color system in layout-v2.ts with pre-computed RGB tuple constants including the full planned palette.

Purpose: Eliminate dead code that could cause accidental V1 activation, and modernize the color system so all future page builders use type-safe, zero-overhead RGB constants instead of runtime hex parsing.

Output: Clean codebase with no V1 files, updated barrel exports/scripts/tests, and a complete RGB color palette ready for all planned PDF sections.
</objective>

<execution_context>
@/Users/ryanbrazzell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanbrazzell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cleanup-foundation/01-RESEARCH.md

# Source files being modified
@web/src/lib/pdf/layout-v2.ts
@web/src/lib/pdf/index.ts
@web/src/lib/pdf/generator-v2.ts
@web/scripts/generate-sample-pdf.ts
@web/scripts/send-test-email.ts
@web/src/app/api/generate-pdf/__tests__/route.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete V1 files and clean all V1 references</name>
  <files>
    web/src/lib/pdf/generator.ts
    web/src/lib/pdf/layout.ts
    web/src/lib/pdf/__tests__/generator.test.ts
    web/src/lib/pdf/__tests__/layout.test.ts
    web/src/lib/pdf/__tests__/integration.test.ts
    web/src/lib/pdf/index.ts
    web/scripts/generate-sample-pdf.ts
    web/scripts/send-test-email.ts
    web/src/app/api/generate-pdf/__tests__/route.test.ts
  </files>
  <action>
    **Step 1 — Delete 5 V1 files:**
    - `web/src/lib/pdf/generator.ts` (349 lines, V1 generator)
    - `web/src/lib/pdf/layout.ts` (994 lines, V1 layout)
    - `web/src/lib/pdf/__tests__/generator.test.ts` (182 lines, V1 generator tests)
    - `web/src/lib/pdf/__tests__/layout.test.ts` (365 lines, V1 layout tests)
    - `web/src/lib/pdf/__tests__/integration.test.ts` (294 lines, V1 integration tests)

    **Step 2 — Update `web/src/lib/pdf/index.ts`:**
    - Remove all V1 re-exports (the `export { addPDFHeader, addExecutiveSummary, ... } from './layout'` block and `export { generatePDF } from './generator'` line and `export type { TaskSectionResult } from './layout'` line)
    - Replace with V2 re-exports: `export { generateTimeFreedomReport, type PDFReportData, type PDFTask } from './layout-v2'` and `export { generatePDFV2 } from './generator-v2'`
    - Keep the S3 export unchanged: `export { uploadToS3, generateSafeFilename } from './s3Service'`

    **Step 3 — Update `web/scripts/generate-sample-pdf.ts`:**
    - Change the import from `'../src/lib/pdf/generator'` to `'../src/lib/pdf/generator-v2'`
    - Update the imported function name from `generatePDF` to `generatePDFV2`
    - Update the function call(s) accordingly (the function signature differs — V2 takes `(taskResult, leadData, options)` instead of V1's `(data, options)`)
    - If adapting the call site is non-trivial, add a `// TODO: Update sample data to V2 format` comment and ensure the file at least compiles by importing the correct module

    **Step 4 — Update `web/scripts/send-test-email.ts`:**
    - Same as Step 3: change import from `'../src/lib/pdf/generator'` to `'../src/lib/pdf/generator-v2'`, update function name and call site

    **Step 5 — Update `web/src/app/api/generate-pdf/__tests__/route.test.ts`:**
    - Find the mock/import that references `@/lib/pdf/generator` and change it to `@/lib/pdf/generator-v2`
    - Update the mocked function name from `generatePDF` to `generatePDFV2`
    - Verify the mock return shape matches V2's `PDFGenerationResult` (same interface, should be compatible)

    **CRITICAL: Do NOT touch `web/src/lib/pdf/generator-v2.ts`, `web/src/lib/pdf/design-system.ts`, `web/src/lib/pdf/s3Service.ts`, `web/src/app/api/generate-pdf/route.ts`, or any file outside the PDF subsystem. The production route already uses V2 and must not be modified.**
  </action>
  <verify>
    1. Run `ls web/src/lib/pdf/generator.ts web/src/lib/pdf/layout.ts 2>&1` — both should show "No such file"
    2. Run `ls web/src/lib/pdf/__tests__/generator.test.ts web/src/lib/pdf/__tests__/layout.test.ts web/src/lib/pdf/__tests__/integration.test.ts 2>&1` — all three should show "No such file"
    3. Run `grep -r "from.*pdf/generator'" web/src/ web/scripts/ --include='*.ts' 2>/dev/null` — should return zero matches (no remaining V1 imports). Note: `generator-v2` matches are expected and correct.
    4. Run `grep -r "from.*pdf/layout'" web/src/ web/scripts/ --include='*.ts' 2>/dev/null` — should return zero matches (no remaining V1 layout imports). Note: `layout-v2` matches are expected and correct.
    5. Run `npx tsc --noEmit` from the `web/` directory — should compile without errors related to missing V1 modules
  </verify>
  <done>
    All 5 V1 files deleted. All 4 reference files updated to point at V2. Zero remaining imports reference V1 module paths. TypeScript compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hex color system with RGB tuple constants and extend palette</name>
  <files>
    web/src/lib/pdf/layout-v2.ts
  </files>
  <action>
    **Step 1 — Define RGB type and color constant object at the top of layout-v2.ts:**

    Replace the existing `COLORS` object (lines 23-32) with:

    ```typescript
    /** RGB color tuple type for jsPDF direct calls */
    type RGB = readonly [number, number, number];

    /** Pre-computed RGB color constants — no runtime hex parsing */
    export const C = {
      // Core V2 design system
      white:        [255, 255, 255] as const satisfies RGB,
      ink:          [17, 24, 39]    as const satisfies RGB,  // #111827
      inkSecondary: [75, 85, 99]    as const satisfies RGB,  // #4B5563
      inkMuted:     [156, 163, 175] as const satisfies RGB,  // #9CA3AF
      accent:       [13, 115, 119]  as const satisfies RGB,  // #0D7377
      accentLight:  [230, 244, 244] as const satisfies RGB,  // #E6F4F4
      divider:      [229, 231, 235] as const satisfies RGB,  // #E5E7EB
      background:   [249, 250, 251] as const satisfies RGB,  // #F9FAFB

      // Cover page
      coverBg:     [13, 115, 119]   as const satisfies RGB,  // Teal (same as accent)

      // Framework page
      frameworkBg: [17, 24, 39]     as const satisfies RGB,  // Dark ink

      // Core Four area accents (placeholder — confirm with design before Phase 3)
      emailAccent:    [59, 130, 246]  as const satisfies RGB,  // Blue #3B82F6
      calendarAccent: [168, 85, 247]  as const satisfies RGB,  // Purple #A855F7
      personalAccent: [234, 179, 8]   as const satisfies RGB,  // Amber #EAB308
      businessAccent: [34, 197, 94]   as const satisfies RGB,  // Green #22C55E

      // CTA page
      ctaBg:   [13, 115, 119]  as const satisfies RGB,  // Teal
      ctaText: [255, 255, 255] as const satisfies RGB,  // White
    } as const;
    ```

    **Step 2 — Replace every `setColor(doc, COLORS.xxx, 'fill')` call with `doc.setFillColor(...C.xxx)`:**

    The migration pattern is:
    - `setColor(doc, COLORS.ink, 'text')` → `doc.setTextColor(...C.ink)`
    - `setColor(doc, COLORS.accent, 'fill')` → `doc.setFillColor(...C.accent)`
    - `setColor(doc, COLORS.divider, 'draw')` → `doc.setDrawColor(...C.divider)`
    - `setColor(doc, '#FFFFFF', 'text')` → `doc.setTextColor(...C.white)` (any inline hex strings get mapped to a named constant)
    - `setColor(doc, '#000000', 'text')` → If #000000 is used, add a `black: [0, 0, 0]` entry to C. Check before assuming.

    There are approximately 55 call sites. Migrate ALL of them. Do not leave any `setColor` calls.

    **Step 3 — Handle any inline hex literals not in COLORS:**
    - Search for any `setColor(doc, '#...'` calls that use hex strings not from the COLORS object
    - For each, either map to an existing C constant or add a new named constant

    **Step 4 — Delete the `setColor` function entirely** (lines 47-58). It should have zero remaining callers after Step 2.

    **Step 5 — Delete the old `COLORS` object** (lines 23-32, now replaced by C in Step 1). The new `C` export replaces it.

    **Step 6 — Verify no hex strings remain in color-setting calls:**
    - Grep layout-v2.ts for any remaining `setColor(` — must be zero
    - Grep layout-v2.ts for any `'#` references in setFillColor/setTextColor/setDrawColor calls — must be zero
    - The only hex strings remaining should be in comments (documenting what each RGB value corresponds to)

    **IMPORTANT: Do NOT change any layout logic, positioning, font sizes, text content, or function signatures. This task is purely a color system migration — the visual output of the PDF must be pixel-identical after this change.**
  </action>
  <verify>
    1. Run `grep -c 'setColor(' web/src/lib/pdf/layout-v2.ts` — should return 0
    2. Run `grep 'COLORS\.' web/src/lib/pdf/layout-v2.ts` — should return 0 (old COLORS object gone)
    3. Run `grep -c 'satisfies RGB' web/src/lib/pdf/layout-v2.ts` — should return a number >= 18 (at least 18 color constants: 8 core + 1 cover + 1 framework + 4 Core Four + 2 CTA + any extras)
    4. Run `grep -c 'doc\.set.*Color(' web/src/lib/pdf/layout-v2.ts` — should be roughly 55+ (direct jsPDF calls replacing the old setColor wrapper)
    5. Run `npx tsc --noEmit` from the `web/` directory — should compile without errors
    6. Run `grep '\.\.\.C\.' web/src/lib/pdf/layout-v2.ts | head -5` — should show spread syntax pattern like `...C.ink`
  </verify>
  <done>
    The hex-string COLORS object and setColor function are deleted from layout-v2.ts. All ~55 color call sites use direct jsPDF methods with spread from the C constant object. The C object exports RGB tuples for all 8 existing colors plus coverBg, frameworkBg, 4 Core Four accents (emailAccent, calendarAccent, personalAccent, businessAccent), ctaBg, and ctaText. TypeScript compiles cleanly. No hex strings appear in any color-setting calls.
  </done>
</task>

</tasks>

<verification>
1. **V1 elimination:** `ls web/src/lib/pdf/generator.ts web/src/lib/pdf/layout.ts 2>&1` shows "No such file" for both
2. **V1 test elimination:** `ls web/src/lib/pdf/__tests__/generator.test.ts web/src/lib/pdf/__tests__/layout.test.ts web/src/lib/pdf/__tests__/integration.test.ts 2>&1` shows "No such file" for all three
3. **No V1 imports remain:** `grep -r "from.*pdf/generator'" web/src/ web/scripts/ --include='*.ts'` returns zero matches; `grep -r "from.*pdf/layout'" web/src/ web/scripts/ --include='*.ts'` returns zero matches
4. **Zero setColor calls:** `grep -c 'setColor(' web/src/lib/pdf/layout-v2.ts` returns 0
5. **Color palette complete:** `grep 'satisfies RGB' web/src/lib/pdf/layout-v2.ts` shows at least 18 named color constants including emailAccent, calendarAccent, personalAccent, businessAccent
6. **TypeScript compiles:** `cd web && npx tsc --noEmit` exits 0
7. **Production route unchanged:** `git diff web/src/app/api/generate-pdf/route.ts` shows no changes
</verification>

<success_criteria>
- V1 generator.ts and layout.ts deleted from codebase
- V1 test files (3) deleted from codebase
- All 4 reference files (index.ts, 2 scripts, route.test.ts) updated to V2
- COLORS hex object replaced by C RGB tuple object in layout-v2.ts
- setColor function deleted from layout-v2.ts
- All ~55 setColor call sites migrated to direct doc.setFillColor/setTextColor/setDrawColor with spread
- Color palette includes: 8 core + coverBg + frameworkBg + 4 Core Four accents + ctaBg + ctaText
- TypeScript compiles cleanly
- Zero remaining V1 imports in the codebase
</success_criteria>

<output>
After completion, create `.planning/phases/01-cleanup-foundation/01-01-SUMMARY.md`
</output>
