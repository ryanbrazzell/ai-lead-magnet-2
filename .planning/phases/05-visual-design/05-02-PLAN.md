---
phase: 05-visual-design
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - web/src/lib/pdf/layout-v2.ts
autonomous: true

must_haves:
  truths:
    - "Each Core Four section in the PDF uses a distinct accent color (different from the placeholder blue/purple/amber/green)"
    - "The cover/ROI page visual treatment creates a sense of financial pain and urgency"
    - "The framework page visual treatment feels like a premium consulting deliverable"
    - "The task pages feel denser and more overwhelming than the other pages"
    - "The CTA page has dramatically more white space than the task pages, creating visual relief"
    - "The CTA button uses the finalized color and sizing from the design decisions"
    - "The PDF renders without Y-overflow errors on any page"
  artifacts:
    - path: "web/src/lib/pdf/layout-v2.ts"
      provides: "All visual design changes applied -- colors, spacing, typography, section treatments"
      contains: "emailAccent"
  key_links:
    - from: "05-01-DESIGN-DECISIONS.md"
      to: "web/src/lib/pdf/layout-v2.ts"
      via: "Implementation Cheat Sheet values applied to C constants and renderer functions"
      pattern: "emailAccent|calendarAccent|personalAccent|businessAccent"
---

<objective>
Apply all finalized design decisions from the adversarial process (Plan 05-01) to the PDF codebase.

Purpose: The design decisions are documented parameter values -- this plan translates them into code changes in `layout-v2.ts`. Every color constant, spacing value, font size, and renderer parameter identified in the DESIGN-DECISIONS.md Implementation Cheat Sheet is applied.

Output: Updated `web/src/lib/pdf/layout-v2.ts` with all visual design changes applied.
</objective>

<execution_context>
@/Users/ryanbrazzell/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ryanbrazzell/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-visual-design/05-01-DESIGN-DECISIONS.md
@.planning/phases/05-visual-design/05-01-SUMMARY.md
@web/src/lib/pdf/layout-v2.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply color system and global design token changes</name>
  <files>web/src/lib/pdf/layout-v2.ts</files>
  <action>
Read the Implementation Cheat Sheet from `.planning/phases/05-visual-design/05-01-DESIGN-DECISIONS.md`. Apply ALL color constant changes to `layout-v2.ts`.

Specifically:
1. **Update the color constants object** (currently `COLORS` with hex values, or `C` with RGB tuples if Phase 1 has run):
   - Update Core Four accent colors: `emailAccent`, `calendarAccent`, `personalAccent`, `businessAccent` with the finalized RGB values from the design decisions
   - Add Core Four accent light variants: `emailAccentLight`, `calendarAccentLight`, `personalAccentLight`, `businessAccentLight` with the finalized light RGB values
   - Update `ctaBg` with the finalized CTA button color
   - Update any other color constants specified in the Implementation Cheat Sheet (e.g., `costRed` for cover page cost emphasis)

2. **Update global spacing values** if the design decisions specify changes to:
   - MARGIN, CONTENT_WIDTH (unlikely to change but check)
   - Any globally-referenced gap values

3. **Preserve all existing rendering logic** -- this task changes VALUES (colors, constants), not STRUCTURE (function signatures, control flow).

**CRITICAL:** After modifying color constants, verify that every reference to the modified constants still makes sense. For example, if `accent` was teal and is now changed, ensure that every `C.accent` or `COLORS.accent` usage still intends to use that color.

**Do NOT modify any function signatures, add new functions, or change the page builder structure in this task.** Color and token changes only.
  </action>
  <verify>
1. The color constants object contains the finalized Core Four accent colors (4 primary + 4 light variants)
2. The CTA button color constant matches the design decision
3. All RGB tuples are valid (0-255 range for each channel)
4. No TypeScript compilation errors: `npx tsc --noEmit --project web/tsconfig.json 2>&1 | head -20`
  </verify>
  <done>
All color constants and global design tokens in layout-v2.ts match the finalized values from DESIGN-DECISIONS.md. No rendering logic changed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Apply per-page visual treatments and emotional differentiation</name>
  <files>web/src/lib/pdf/layout-v2.ts</files>
  <action>
Apply the per-page visual treatment changes specified in the DESIGN-DECISIONS.md. This task modifies RENDERER PARAMETERS within existing functions -- font sizes, spacing values, fill colors, text colors, and element dimensions.

**Cover/ROI page (Dimension 3 decisions):**
- Apply financial pain emphasis: update cost figure text color (likely to costRed), adjust hero metric font size/weight if specified, modify investment block visual treatment per the decision
- These changes target `buildSummaryPage` / `renderHeroMetric` / `renderInvestmentBlock` (or equivalent functions)

**Framework page (Dimension 4 decisions):**
- Apply trust/education aesthetic: update Three Pillars section styling, Core Four box styling (accent bar width, fill colors, text weight)
- These changes target `buildFrameworkPage` / related renderer functions

**Task pages (Dimension 5 decisions):**
- Apply density/overwhelm treatment: update task card vertical spacing (gap after divider), section header bar height/color treatment, task card internal padding
- Apply typography weight changes per Dimension 7 decisions (e.g., lighter body text weight on task pages)
- These changes target `renderCoreFourSection` / `renderTaskCard` / `buildCoreFourTaskPages` (or equivalent functions)

**CTA page (Dimension 6 decisions):**
- Apply openness/relief treatment: update CTA block spacing, button dimensions (width, height, corner radius), headline font size/weight, white space distribution
- Apply typography weight changes per Dimension 7 decisions (e.g., heavier headline weight on CTA page)
- These changes target `buildCTAPage` / `renderCTABlock` (or equivalent functions)

**Footer/Header (Dimension 8 decisions):**
- Apply footer/header treatment changes: font size, color, divider line weight, running header addition if decided
- These changes target `renderFooter` / `renderHeader` (or equivalent functions)

**CRITICAL Y-BUDGET CHECK:**
After making spacing and font size changes:
- Framework page: verify all content fits within ~250mm usable height (page height 297mm minus header/footer/margins). If the framework page was tight before, any font size increase or spacing increase could overflow. Calculate the new total height.
- Task pages: safe because they use checkPageBreak, but verify the section header + first task card fit together (checkPageBreak should request enough space for both)
- Cover page: verify content does not extend past y=270mm
- CTA page: simple layout, verify button and text fit

**IMPLEMENTATION APPROACH:**
- Work through the Implementation Cheat Sheet line by line
- For each change, find the exact location in layout-v2.ts where the current value exists
- Replace the current value with the decision value
- If a renderer function needs a new parameter (e.g., a per-section spacing value), add it minimally
- Do NOT refactor or restructure functions -- make targeted value changes
  </action>
  <verify>
1. Cover page uses the specified emotional treatment (cost figure colors, hero metric sizing)
2. Framework page uses the specified trust/education treatment
3. Task page section headers use Core Four accent colors
4. Task card spacing matches the decision value
5. CTA button uses the finalized color, dimensions, and corner radius
6. Footer/header matches the decision treatment
7. No TypeScript errors: `npx tsc --noEmit --project web/tsconfig.json 2>&1 | head -20`
8. Build succeeds: `cd web && npm run build 2>&1 | tail -10`
  </verify>
  <done>
All per-page visual treatments from DESIGN-DECISIONS.md are applied. The cover page creates urgency, the framework page feels premium, task pages feel dense/overwhelming, and the CTA page provides visual relief. The emotional arc (pain -> education -> overwhelm -> solution) is reflected in distinct visual treatments across pages.
  </done>
</task>

</tasks>

<verification>
- [ ] All 8 design dimensions from DESIGN-DECISIONS.md are reflected in layout-v2.ts code
- [ ] Core Four accent colors are distinct and match the finalized palette
- [ ] Cover page uses urgency/pain visual treatment
- [ ] Framework page uses trust/education visual treatment
- [ ] Task pages use density/overwhelm visual treatment
- [ ] CTA page uses openness/relief visual treatment
- [ ] Typography weight varies per page type as specified
- [ ] No Y-overflow on any page (especially framework page)
- [ ] TypeScript compiles without errors
- [ ] Build succeeds
</verification>

<success_criteria>
1. Every value from the DESIGN-DECISIONS.md Implementation Cheat Sheet is applied to layout-v2.ts
2. The PDF's visual design creates distinct emotional tones per section (DESIGN-03)
3. Each Core Four area has a distinct accent color (DESIGN-02)
4. The design was determined through the adversarial process in Plan 05-01 (DESIGN-01)
5. The project builds and the PDF renders without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-visual-design/05-02-SUMMARY.md`
</output>
